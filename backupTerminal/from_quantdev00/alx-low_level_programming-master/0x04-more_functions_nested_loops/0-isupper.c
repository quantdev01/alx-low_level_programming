#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * _isupper - return if the number is upper'
 *@c: the number
 * Return: 0;
 */
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
		return (0);
	return (0);
}
