#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * _isdigit - return if the number is upper'
 *@c: the number
 * Return: 0;
 */
int _isdigit(int c)
{
	if (isdigit(c))
	{
		return (1);
	}
	else
		return (0);
	return (0);
}
